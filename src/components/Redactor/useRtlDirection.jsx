import React from 'react';
import PropTypes from 'prop-types';

export default function useRtlDirection() {
  return <></>;
}

useRtlDirection.defaultProps = {
 text: '',
};

useRtlDirection.propTypes = {
  /** text in the editor */
  text: PropTypes.string,
};
